import type { TableSize } from '../../typings'

export const tableSizeOptions: { key: TableSize; label: string }[] = [
  { key: 'small', label: '紧凑' },
  { key: 'medium', label: '中等' },
  { key: 'large', label: '宽松' }
]
